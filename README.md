# Survivor 51 Fantasy League

**This site has moved.**

The league now lives at **https://survivor51.matusov.workers.dev/**

It moved off GitHub Pages because Pages cannot store data — picks and commissioner
results need a real database so everyone sees the same thing. The app is now served
by Cloudflare Workers with Cloudflare KV as the shared store.

`index.html` here is just a redirect. Nothing else lives in this repo.
